// Copy this file to config.js and fill in your Supabase project's values.
// The anon key is safe to expose publicly (it's what Supabase's row-level
// security policies are for) - do not put the service role key here, that
// one stays server-side with the bot only.
export const SUPABASE_URL = 'https://YOUR-PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY';
